class Scene {
    constructor(name, interacts) {
        this.name = name;
        this.interacts = interacts; //array of all interactable items in that scene
        this.w = 500; //width of canvas
        this.h = 500; //height of canvas
        this.walls = 3; //amount of walls in the main room - 0, 1, 2, 3
    }

    createScenes() {

    }
}